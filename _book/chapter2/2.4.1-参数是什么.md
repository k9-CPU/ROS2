# 2.4.1 参数是什么

参数（Parameter）是**节点对外暴露的动态配置项**，你可以在运行时修改参数值，从而改变节点的行为，无需重新编译或重启节点。

想象这样一个场景：你在调试一个PID控制器，每次调整参数都需要修改源码、重新编译、重新运行——效率极低。如果将PID的三个增益值声明为参数，你就可以直接在命令行中实时修改，节点会立即响应新的配置。

参数的应用场景非常广泛：控制循环频率、设置阈值、启用/禁用功能开关等。

ROS2参数支持多种数据类型：

1. **整型（integer）**
1. **浮点型（double）**
1. **布尔型（bool）**
1. **字符串（string）**
1. **数组（byte_array, bool_array, integer_array, double_array, string_array）**

参数既可以保存在节点内部，也可以持久化到YAML文件中，便于批量配置和版本管理。

通俗的讲，参数就像手机里的“设置”——你可以随时改铃声大小，不用重启手机。

![image-20260426154322325](./2.4.1-%E5%8F%82%E6%95%B0%E6%98%AF%E4%BB%80%E4%B9%88.assets/image-20260426154322325.png)
